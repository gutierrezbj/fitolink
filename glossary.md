# FitoLink — Glossary

## Satellite & Remote Sensing

| Term | Definition |
|------|-----------|
| **NDVI** | Normalized Difference Vegetation Index = (B08-B04)/(B08+B04). Range [-1,1]. Core health indicator. Thresholds: <0.30 critical, <0.40 high risk, <0.55 medium, ≥0.55 healthy. |
| **NDRE** | Normalized Difference Red Edge = (B08-B05)/(B08+B05). More sensitive to chlorophyll stress than NDVI. Detects stress before it's visible in NDVI. Requires Sentinel-2 B05 (RedEdge band, 20m). |
| **Sentinel-2 L2A** | ESA satellite. 10m resolution (B04/B08), 20m (B05/B11/B12). 5-day revisit. Bands: B04=Red, B05=RedEdge, B08=NIR, SCL=Scene Classification. |
| **SCL** | Scene Classification Layer — Sentinel-2 band classifying each pixel: 0=no data, 1=saturated, 3=cloud shadow, 8=cloud medium, 9=cloud high, 10=cirrus, 11=snow. Used for cloud masking. |
| **CDSE** | Copernicus Data Space Ecosystem — ESA/EU platform providing Sentinel data access. Both OData catalog and openEO endpoint. Same credentials for both. |
| **openEO** | Standard protocol for cloud-side satellite processing. FitoLink sends a process graph to CDSE openEO, executes NDVI/NDRE in the cloud, receives small GeoTIFF in memory (no 500MB downloads). |
| **OData** | Legacy Sentinel-2 download API. Downloads 500MB SAFE ZIPs. Used as fallback when openEO is unavailable. |
| **GeoTIFF** | Georeferenced raster format. FitoLink receives NDVI results as GeoTIFF in BytesIO (in-memory, no disk I/O). |
| **SAFE ZIP** | Sentinel-2 product archive format (~500MB). Contains JP2 band files in 10m/20m/60m resolutions. Used in OData fallback path. |
| **Max composite** | Temporal reduction strategy: picks the maximum NDVI value per pixel across the time window. Selects the clearest (least cloudy) observation. |
| **CLMS NPP** | Copernicus Land Monitoring Service Net Primary Productivity. Based on FAPAR 200m. Complements NDVI with real vegetation productivity. |
| **FIRMS** | Fire Information for Resource Management System (NASA). Near real-time fire detection. Future integration for burn alerts. |
| **MPC** | Microsoft Planetary Computer. Catálogo STAC público con auto-firma de URLs. Sin auth obligatoria, sin coste. Acceso a MODIS, Landsat, Sentinel, ERA5, TerraClimate, etc. Integrado en FitoLink desde Sprint MPC (2026-05-02). |
| **MODIS / MOD13Q1.061** | Terra MODIS Vegetation Indices. 250m resolution, 16-day composite, 2000-presente, global. NDVI con scale factor 0.0001 e int16 fill -3000. Usado en FitoLink para baseline histórico 3-5 años por parcela. |
| **STAC** | SpatioTemporal Asset Catalog. Estándar abierto para indexar datos geoespaciales. Permite buscar por bbox + datetime + collection. MPC, CDSE y NASA exponen catálogos STAC. |
| **odc-stac** | Open Data Cube + STAC: librería Python para cargar items STAC como xarray DataArray. Usada en FitoLink para extraer time series MODIS por parcela. |
| **TerraClimate** | Dataset mensual climatología 1958-2021, ~4km global. Variables: precip, tmax, tmin, PDSI (drought index), PET, AET. Vía MPC. Fallback de baseline climático en FitoLink (la fuente principal es Open-Meteo). |
| **Open-Meteo** | API HTTP gratuita para datos meteorológicos históricos (ERA5 reanalysis 1940-presente) y actuales. Sin API key. Tier gratis 10k requests/día. Usado en FitoLink para baseline climático (1995-2024) y clima 30-day actual. Rate-limit 429 manejado con backoff exponencial 5/10/15/20s. |
| **ERA5** | Reanálisis ECMWF horario 1940-presente, ~31km global. Backend de Open-Meteo y de varios datasets MPC. Provee T, lluvia, viento, ET₀ FAO. |
| **ET₀ (FAO)** | Evapotranspiración de referencia, método Penman-Monteith FAO. Demanda atmosférica de agua. Calculada por Open-Meteo desde ERA5. Indicador clave de estrés hídrico. |
| **PDSI** | Palmer Drought Severity Index. Rango -10 (sequía extrema) a +10 (humedad extrema). En TerraClimate. No disponible en Open-Meteo (FitoLink usa anomalía precipitación como proxy). |

## ML & Anomaly Detection

| Term | Definition |
|------|-----------|
| **V1 detector** | Threshold-based anomaly detector. Simple rules: if NDVI drops > X → alert. More false positives. Used as fallback. |
| **V2 detector** | RandomForestClassifier (scikit-learn). 150 trees, 8 max depth, class_weight='balanced'. Trained on synthetic NDVI time series. 4 output classes. |
| **NdviFeatures** | 13-element feature vector extracted from NDVI time series: current_ndvi, below_critical, below_high, delta_1, delta_3, delta_5, slope_3, slope_5, drop_from_recent_max, consecutive_drops, volatility, ndre_delta_1, history_length. |
| **delta_1** | Current NDVI minus previous reading (~5 days). Negative = drop. Key short-term change signal. |
| **consecutive_drops** | Number of consecutive declining readings. ≥3 → alert_type='stress_pattern'. |
| **Severity classes** | 0=no anomaly (healthy), 1=medium, 2=high, 3=critical. Mapped to alert severity field. |
| **alert_type** | 'ndvi_drop' (sudden fall), 'stress_pattern' (sustained decline, consecutive_drops≥3), 'ndre_anomaly' (chlorophyll stress, ndre-ndvi gap>0.08). |
| **aiConfidence** | Probability of the predicted class from RandomForest predict_proba. Stored in alert doc, displayed as bar in UI. |
| **modis_baseline_ndvi** | Argumento opcional del detector V2: media histórica MODIS de 5 años para esa parcela en ese mes. Si la lectura actual está dentro de ±15% del baseline, override "anomalía → no anomalía" (suprime falsos positivos en parcelas con dinámica estacional fuerte). |
| **drought_flag** | Argumento opcional del detector V2: 'none' / 'mild' / 'moderate' / 'severe' calculado de `precip_30d / climate_normal_precip`. Enriquece el texto de la alerta con contexto físico para distinguir estrés biótico de abiótico. |
| **modisBaseline** | Subdoc en `parcels` con baseline NDVI MODIS. Schema: `{source, years, computed, months[{month, mean, std, n}], allTimeMean, observationCount}`. Construido por `build_mpc_baselines.py`. |
| **climateBaseline** | Subdoc en `parcels` con normales climáticas mensuales. Schema: `{source, period, computed, months[{month, precip, tmax, tmin, pdsi, pet}], annualPrecip, annualPet, aridityIndex}`. Fuente primaria: Open-Meteo ERA5 1995-2024. |
| **recentClimate** | Subdoc en `parcels` con clima últimos 30 días (refrescado cada pipeline run). Schema: `{source, days, fetched, precipTotalMm, tempMeanC, tempMaxC, tempMinC, et0TotalMm, daysWithRain, lastRainDaysAgo, precipPctOfNormal, precipAnomalyMm, tempAnomalyC, droughtFlag}`. |
| **search_with_retry** | Helper en `planetary_computer.py` para STAC searches con backoff exponencial 2s/4s ante "maximum allowed time" timeouts típicos del MPC anonymous tier. |

## Agricultural Domain

| Term | Definition |
|------|-----------|
| **Parcela** | Agricultural plot/field. Has geometry (GeoJSON Polygon), owner (farmer), crop type, province, NDVI history. |
| **Cultivo / cropType** | Crop type: 'olivo' (olive), 'vinedo' (vineyard), 'cereal', 'citricos', etc. |
| **NDVI history** | Time series of NDVI readings stored per parcel in MongoDB (ndviHistory array). Each reading: date, mean, min, max, anomalyDetected, source, optional ndreValue. |
| **Alerta** | Anomaly alert generated when V2 detector flags a parcel. Fields: parcelId, type, severity, ndviValue, ndviDelta, aiConfidence, detectedAt, status, imagery. |
| **Operacion** | Service request from farmer. Lifecycle: requested → assigned → in_progress → completed/cancelled. Has flightLog, product, weatherConditions, prescription. |
| **flightLog** | Flight execution data: startTime, endTime, areaHa treated. Required to complete an operation. |
| **Fitosanitario** | Phytosanitary product/treatment. Registered applicators required (ROPO license). |
| **SIGPAC** | Spanish agricultural plot reference system. `sigpacRef` field on parcels for official ID. |
| **ROPO** | Registro Oficial de Productores y Operadores — Official registry for phytosanitary product users. Pilots applying treatments must be registered. |
| **Perito agricola** | Agricultural damage assessor for insurance claims. 450 in Spain for 113,000+ annual claims. |
| **Agroseguro** | Pool of 23 Spanish agricultural insurance companies managing ~1B EUR/year in premiums. Key B2B target. |

## Platform & Architecture

| Term | Definition |
|------|-----------|
| **SDD** | Software Development with AI Direction — SRS methodology. Human directs, AI executes. |
| **B2B / B2C** | B2C: farmers and pilots. B2B: insurers (Agromutua, Agroseguro) paying per monitored hectare. |
| **Demo mode** | Activated via `?demo` URL param. Shows 4 demo login buttons (farmer/pilot/insurer/admin). Works on staging and production. |
| **Seed** | `npm run seed --workspace=apps/api` — resets all data and creates 4 demo users, 3 parcels, 3 alerts, 4 operations. |
| **Toast** | UI notification (Zustand toastStore + ToastContainer). Auto-dismiss in 4s. Fires on: accept assignment, reject, request service, complete operation, false positive. |
| **HealthScoreGauge** | SVG circular gauge 0-100 derived from NDVI. Color bands: green ≥60, yellow ≥40, orange ≥25, red <25. |
| **NdviChart** | Recharts AreaChart with gradient fill, min/max range band, anomaly dots (red ring), NDRE dashed purple line, reference lines at 0.30 (critical) and 0.40 (alert). |
| **OperationKanban** | Kanban board component. Farmer: 4 cols (Solicitada/Asignada/En curso/Completada). Pilot: 3 cols (Pendiente/En curso/Completada) with inline accept/reject. |
| **AlertPulse** | Double CircleMarker (dashed outer + solid inner) on ParcelMap for parcels with active alert or NDVI<0.30. |
| **Auto-assign** | On operation creation, pipeline does $near query (2dsphere, 100km radius) to find nearest verified pilot. Fallback: any verified pilot. Operation goes to 'assigned' status immediately. |
| **2dsphere index** | MongoDB geospatial index on User.location and Parcel.geometry. Required for $near auto-assign queries. |
| **deploy.sh** | Script en `/opt/fitolink/deploy.sh`. Ejecuta: git pull → docker compose build → docker compose up -d → seed via container temporal. Invocado via SSH desde Mac. |
| **Docker Compose** | Orquestacion de contenedores en staging: `fitolink-web` (nginx:alpine + Vite dist), `fitolink-api` (node:22-alpine), `fitolink-mongo` (mongo:7). Red interna `fitolink_default`. |
| **SVG icons** | Iconos custom en `apps/web/public/`. Convención: paths que empiezan con `/` en DashboardLayout renderizan `<img>` en vez de emoji `<span>`. Archivos: farmer, drone-pilot, drone, insurance2, location, operational-system, siren, system-administration, user, setting, smart-farming, vegetables, favicon. |
| **FitAllButton** | Componente Leaflet control (topright) en ParcelMap. Botón "⊙ Ver todas" que llama `map.fitBounds()` sobre todas las parcelas con padding 40px. Permite resetear el zoom tras seleccionar una parcela. |
| **AdminUsersPage cards** | Rediseño de AdminUsersPage con grid de tarjetas (1→2→3 cols) en vez de filas horizontales. UserCard con iniciales, SVG de rol, badge verificado, certs, rating y fecha. |
| **NdviHeatmap** | Overlay Leaflet/react-leaflet en ParcelDetailPage. GeoJSON FeatureCollection de celdas Polygon (~10m, CELL_HALF=0.000045°). Coloreadas por ndviToColor(). Toggle button "Mapa NDVI" visible si existe snapshot. |
| **NdviLegend** | Leyenda visual del heatmap NDVI. 6 rangos con color, etiqueta y descripción. Se muestra sobre el mapa (absolute bottom-right, z-1000) cuando el toggle está activo. |
| **ndvi_snapshots** | MongoDB collection para snapshots intra-parcela. Schema: parcelId, date, resolution, points[{lat,lng,ndvi}], bbox, pixelCount. Índice compound {parcelId:1, date:-1}. |
| **RBF interpolation** | Radial Basis Function (scipy, kernel=thin_plate_spline). Interpola pixels NDVI muestreados del GeoTIFF a grilla uniforme. Proyecta a UTM EPSG:25830 para interpolación métrica, devuelve a WGS84. |
| **PAC compliance** | FitoLink como "proveedor de evidencia técnica para cumplimiento PAC". Historial NDVI = prueba pre-siniestro. Cuaderno de campo digital. Trazabilidad fitosanitaria 100%. Sección PacPain en LandingPage. |
| **MpcContextWidget** | Componente React en `apps/web/src/features/parcels/MpcContextWidget.tsx`. Tarjeta 3 columnas con cabecera azul-cian gradient (branding Microsoft Planetary Computer). Muestra NDVI baseline 5y MODIS · normales climáticas mensuales · clima últimos 30 días con badge drought (none/mild/moderate/severe). Insertado debajo del NdviChart en ParcelDetailPage. |
| **build_mpc_baselines.py** | Script Python one-shot idempotente. Recorre todas las parcelas activas y construye `modisBaseline` + `climateBaseline` para las que no lo tengan. Flags: `--refresh` (rebuild todos), `--parcel ID` (uno solo). Pacing 2s entre parcelas para evitar rate-limits. Failures por parcela no abortan el batch. |
| **demo-sergio-asaja** | googleId del usuario demo personalizado para Sergio Valverde (Jefe de Formación ASAJA). Rol admin, company "ASAJA". Botón destacado amarillo con anillo en LoginPage cuando se accede con `?demo`. |
| **Onepager ASAJA** | PDF A4 de 1 cara generado con reportlab desde `docs/asaja-onepager/build_onepager.py`. Filosofía de diseño "Botanical Telemetry" (lámina botánica científica + telemetría aeroespacial). Tipografía: Crimson Pro + Bricolage Grotesque + IBM Plex Mono. Paleta: verdes FitoLink + amarillo telemetría #d4a017. |
| **Botanical Telemetry** | Filosofía de diseño documentada en `docs/asaja-onepager/design-philosophy.md`. Cruce entre tradición de lámina botánica (Maria Sibylla Merian, Redouté) y disciplina de telemetría aeroespacial. Crosshairs de registro, coordenadas en cabecera, numeración griega/romana. Aplicable a futuros documentos comerciales FitoLink. |
| **gzip sweep SRS** | Activación system-wide de gzip en nginx de srs-staging (2026-05-04). Aplica a todos los sites SRS hospedados allí (fitolink, bodyforge, dbuilder, insiteiq, moevet, ottoia, skypro360, s3.skypro360). Reducción típica JS bundle 71-72%. Backup config en `/etc/nginx/nginx.conf.bak.20260504-094222`. |
| **AgroM** | Vertical agro de SRS. Marca pública `agrom.es` (`.com` cogido). FitoLink es su producto principal. Brazo técnico: `systemrapid.io`. 11-may-2026: identidad visual aplicada al frontend (login + sidebar) y al email transaccional. Tokens Tailwind `colors.agrom.*`. |
| **AgroM Identity Sprint v0.1** | Sistema visual heredado para AgroM. Paleta: deep `#1B4332`, terra `#E07A3C`, ink `#0F2A22`, paper `#F4F0E8`, parch `#E8DDC9`, rule `#C9A876`, muted `#6B6B5C` + semánticos alert/warning/success/info. Tipografía: Fraunces (display) + IBM Plex Sans (body) + IBM Plex Mono (microcopy). |
| **AgroM wordmark** | Logo oficial "Agro·M" con punto terracota entre la O y la M. Archivos en `apps/web/public/brand/agrom-wordmark.{svg,png}` (transparent bg). También monograma en `agrom-monogram.{svg,png}`. Servidos en `https://fitolink.agrom.es/brand/`. |
| **learned baseline** | Override post-hoc del detector V2 que aprende el patrón normal de NDVI de cada parcela individual (últimas 18 lecturas Sentinel-2). Suprime alerta si \|z\| < 1.0 (lectura dentro del rango natural). Escala a 'high' si z < -2.5. Módulo `workers/geo-pipeline/src/alerting/learned_baseline.py`. |
| **Weather widget** | Componente en `apps/web/src/features/weather/WeatherWidget.tsx`. Open-Meteo Forecast 7 días + suitability badges drone por hora (ideal / ok / precaución / no aplicar). Dos modos: fitosanitario e inspección. Reglas AESA/FAO en `suitability.ts`. Integrado en ParcelDetailPage. |
| **AlertsTimeline** | Componente analytics en `apps/web/src/components/AlertsTimeline.tsx`. 3 paneles: stacked bar semanal por severidad + donut + top-5 parcelas problemáticas. Selector temporal 4s/8s/12s/26s. Backend `GET /admin/alerts/timeline?weeks=N`. Patrón heredado de OverWatch. |
| **emailService.ts** | Servicio nodemailer dual-mode en `apps/api/src/services/emailService.ts`. Trigger automático en `createAlert` para severity critical/high (fire-and-forget). Endpoint manual `POST /alerts/:id/resend-email` (admin) con override `{to, name}`. Plantilla HTML editorial AgroM con wordmark + Google Fonts. |
| **SMTP_* env vars** | Variables Gmail SMTP en `.env`: HOST=smtp.gmail.com, PORT=587, USER=juang@systemrapid.io, PASS=`<App Password 16 chars>`, FROM=`FitoLink Alertas <juang@systemrapid.io>`. App Password de Workspace via myaccount.google.com → Security → App passwords. Container Docker lee via `env_file: .env`. |
| **Botanical Cartography** | Filosofía visual en `docs/comercial/design-philosophy.md`. Cruce editorial entre lámina botánica científica + cartografía catastral SIGPAC. Aplicada a la infografía A4 del cliente pistacho. Hairlines, romanos, paleta paper/parch/deep/terra. |
| **Pitch deck cliente pistacho** | Material comercial en `docs/comercial/`. PPTX editable 16:9 wide (5 slides) generado por `build-deck.cjs` (pptxgenjs). PDF cerrado. Infografía A4 PDF + JPG por `build_infografia.py` (reportlab). Genérico, sin nombre del cliente, para evitar polémica con 100x100. |
| **Cooperativa (rol)** | Sexto rol de User: `cooperative`. NO posee parcelas — agrega los datos de farmers con `cooperativeId` apuntando a ella. Endpoint `GET /cooperative/overview`. Componente `CooperativeDashboardHome`. Seed `seedCooperativeSocios.ts` crea 5 socios DCOOP en Estepa. |
| **establishmentPhase** | Campo opcional en Parcel para parcelas recién plantadas o con cultivo residual (NDVI baja = ruido conocido). Con `isActive: false` saca a la parcela de los dashboards de demo. Usado para aislar las parcelas pistacho de Jonh del demo (movidas al user `john-pistacho-real`). |
| **Jonh Yanga Núñez** | Socio operativo de AgroM (autónomo bajo paraguas SRS). Coordinación operativa con clientes. En materiales comerciales aparece como "Coordinación operativa — Jonh Yanga Núñez". |
| **Predictive insights (Ola 1.5)** | Capa nueva de inteligencia accionable sobre los datos que ya tenemos. 4 piezas: (1) Trayectoria NDVI proyectada — regresión lineal. (2) Eventos meteo personalizados — 7 reglas sobre Open-Meteo. (3) Plagas curadas — admin emite RAIF/MAPA, sistema distribuye por crop+radius. (4) Morning digest 7am — email AgroM editorial que orquesta P1+P2+P3. Las cuatro entregadas 12-may-2026. |
| **digestService.ts** | Orquestador Ola 1.5 · Pieza 4 en `apps/api/src/services/digestService.ts`. `composeDigestForUser(userId)` mergea P1 (forecasts) + P2 (weather) + P3 (advisories) en un `DigestPayload` con flag `worthSending`. `sendDigestForUser()` es idempotente intraday vía `User.lastDigestSentAt`. Dedupe weather/pest a nivel usuario para no spammear "tormenta el viernes" × N parcelas. |
| **digestCron.ts** | Scheduler in-process en `apps/api/src/services/digestCron.ts`. setInterval cada 60s chequea reloj UTC; dispara una vez por día cuando cruza 05:00 UTC (=07:00 Madrid verano / 06:00 invierno). Guard `DIGEST_CRON=true` env evita auto-fire en producción hasta validación manual. `startDigestCron()` se llama desde `server.ts` al arrancar el API. |
| **sendMorningDigest CLI** | Runner manual en `apps/api/src/seed/sendMorningDigest.ts`. Flags: `--user <email\|googleId>`, `--all` (default), `--dry-run` (no SMTP, log estructurado), `--force` (saltar idempotencia diaria). Smoke seguro: `docker compose exec -T api node apps/api/dist/seed/sendMorningDigest.js --user john-pistacho-real --dry-run`. |
| **DigestPayload** | Tipo estructurado emitido por `composeDigestForUser`. Campos: user (id/name/email/role), generatedAt, parcelCount, totalHectares, ndviSection (actionable[] + stableCount), weatherSection[], pestSection[], worthSending. La sección NDVI sólo lista forecasts descendentes o ya críticos; las estables sólo cuentan. Si las 3 secciones están vacías de contenido accionable → `worthSending=false` → no se envía. |
| **digestSubscribed / lastDigestSentAt** | Campos opcionales en User para Ola 1.5 · Pieza 4. `digestSubscribed: boolean` (default true) controla opt-out. `lastDigestSentAt: Date` se stampa tras un send exitoso; el chequeo same-day-UTC garantiza idempotencia entre reruns del mismo día. Targeting V1: sólo roles farmer + cooperative. Cuentas demo-* excluidas excepto `john-pistacho-real`. |
| **Agrodex (Gregorio Becerra)** | Empresa de formación agrícola (Gregorio Becerra dueño). 12-may-2026 entregada presentación FitoLink, interés en meterlo en sus cursos para nuevos alumnos. Validación cualitativa importante: prescriptor técnico → embedded marketing con la próxima generación de técnicos de campo. Próximo paso pendiente: definir si cuenta demo dedicada `demo-agrodex-curso-001` + acceso sandbox alumnos. |
| **TermsPage / PrivacyPage** | Páginas estáticas públicas en `apps/web/src/pages/{TermsPage,PrivacyPage}.tsx`. T&C (11 §) + Política Privacidad RGPD (10 §) castellano Tipo A friendly. Identidad AgroM editorial completa. Linkadas desde LoginPage footer, RegisterPage checkbox, LandingPage footer, PricingPage footer. |
| **acceptedTerms / acceptedTermsAt** | Campos en User para audit trail RGPD. `acceptedTerms: boolean` se stampa true cuando el usuario marca el checkbox en /register. `acceptedTermsAt: Date` guarda el ISO timestamp exacto del clic, útil si más adelante actualizamos la versión de T&C y tenemos que reverificar consentimiento. |
| **FarmerEmptyState** | Componente en `DashboardHome.tsx` que sustituye el dashboard cuando `isFarmer && parcels.length === 0`. Wordmark AgroM + 3 pasos romanos I/II/III + CTA grande "Subir mi primera parcela →". Desaparece automáticamente al crear la primera parcela. |
| **Aviso "primera pasada Sentinel-2"** | Bloque informativo en ParcelDetailPage cuando `ndviHistory.length === 0`. Fondo parch + emoji ⌛ + explicación clara de que el satélite pasa cada 5 días. Evita el pánico "no hay datos día 1" en farmers nuevos. |
| **sendWelcomeEmail** | Email transaccional AgroM tras alta de usuario. Dual template: farmer auto-verified (3 pasos + CTA "Subir parcela") vs role que requiere verificación manual (mensaje "validamos en 24-48h"). Disparado fire-and-forget desde `authService.registerWithGoogle`. Nunca throw — alta válida aunque SMTP falle. |
| **sendFirstParcelEmail** | Email transaccional cuando un farmer crea su primera parcela activa. `maybeSendFirstParcelEmail()` cuenta `Parcel.countDocuments({ownerId, isActive: true})` tras el create — si total = lo recién creado, primera vez → manda email con datos registrados + "qué pasa ahora" + CTA "Ver mis parcelas". Idempotente: segunda parcela no dispara. Cubre `createParcel` y `createParcelsBulk` (KMZ import). |
| **PricingPage** | Página pública `/pricing` con modelo C de AgroM. Hero "Primero le demostramos el valor. Después hablamos." + grid 2 columnas: tier free (paleta paper, monitorización satelital + alertas + digest matutino) + tier drone (paleta deep invertida, por hectárea aplicada, sales-led sin precio público). Formulario "Solicitar demo" inline con identidad AgroM. |
| **POST /contact/demo-request** | Endpoint público en `apps/api/src/routes/v1/contact.ts`. Valida con zod, no requiere auth. Dispara `sendDemoRequestEmail()` fire-and-forget que manda a `gutierrezbj@gmail.com` con cc `jawerjohn1993@gmail.com` y reply-to al prospect (click "Responder" cae directo sobre el lead). HTML AgroM editorial con tabla de datos del prospect. |
| **CRITICAL_no_inventar** | Regla operativa nº1 del proyecto en `memory/CRITICAL_no_inventar.md`. Establecida 12-may-2026 tras incidente de publicidad engañosa: yo (asistente) había inventado capacidades en copy comercial (blockchain, eIDAS, cuaderno PAC automático, etc.) que no existen en código. La regla obliga a verificar SIEMPRE contra código antes de afirmar nada en copy comercial. NO inventar, NO rellenar con "lo que sonaría completo". |
| **inferCoverLevel** | Helper compartido en `packages/shared/src/cropContext.ts`. Función única de verdad del estado real del cultivo: `'low' \| 'partial' \| 'full'`. Umbrales: `establishmentPhase=true → 'low'`; NDVI<0.30 → 'low'; 0.30-0.45 → 'partial'; ≥0.45 → 'full'. Usado para suprimir lecturas absolutas en parcelas con suelo dominantemente expuesto (caso pistachar Jonh). |
| **cropContext.ts** | Módulo compartido con helpers: `inferCoverLevel`, `cropLabel` (castellano legible: pistacho→pistachar), `lowCoverNote` (mensaje Tipo A friendly que sustituye al alarmista absoluto). Importado por backend Node + frontend React desde `@fitolink/shared`. |
| **establishmentPhase** | Campo opcional en `Parcel` (Mongoose schema + interface). `true` cuando la parcela está recién plantada o aún sin cobertura completa (residual del cultivo anterior, pistachos jóvenes). Suprime falsos positivos en pipeline V2, badge térmico, mensaje del digest, pill NDVI forecast, diagnóstico isCritical, stressPct intra-parcela. Hoy seteado vía seed `reactivatePistachares.ts` para las 6 parcelas pistacho de Jonh. Futuro: detector V3 lo inferirá auto de NDVI + edad. |
| **detectionFeatures / detectionModel** | Campos opcionales en `Alert` (Sprint feedback loop · paso 2). El detector V2 RandomForest snapshotea las 17 features que generaron cada alerta + el modelo usado ('v1_threshold' \| 'v2_random_forest'). Pareado con `resolvedBy` cuando el farmer cierra la alerta, es ground truth para `retrain_from_ground_truth.py`. |
| **retrain_from_ground_truth.py** | Script Python en `workers/geo-pipeline/src/alerting/` que reentrena el RandomForest V2 con los `Alert.detectionFeatures` resueltos. Mapea `resolvedBy + severity` → label (0/1/2/3). Combina real (repetido REAL_SAMPLE_WEIGHT=3 veces) con dataset sintético. Idempotente, reversible (backup .bak.timestamp), `--dry-run`, MIN_REAL_SAMPLES=20 como guard. |
| **closeEstablishmentFalsePositives** | Seed script idempotente en `apps/api/src/seed/`. Cierra alerts legacy abiertas sobre parcelas en establecimiento como `status: 'resolved', resolvedBy: 'false_positive'`. Ejecutado 13-may en producción: 2 alerts cerradas sobre ZONA 2 pistachar. Las marca para que el retrain script las aprenda como label 0 (healthy). Soporta `--dry-run`. |
| **Identity Ecosystem · FitoLink como base** | Decisión JuanCho 13-may-2026. La identidad visual del ecosistema agrario (FitoLink producto + AgroM empresa + AgroOps producto operacional) se ancla en la paleta + tipografía histórica de FitoLink, no en el Identity Sprint AgroM efímero de mayo. Eliminados los tokens `agrom-*` del Tailwind. FitoLink → AgroM → AgroOps heredan en cascada del mismo sistema. |
| **brand-600** | Verde topographic FitoLink oficial del ecosistema (`#46632e`). El mismo que el botón "Acceder" del header de la LandingPage y la palabra "tratamiento" del título grande. Sustituye al efímero `agrom-deep #1B4332` en todo el frontend, emails y Notion AgroOps. Usado para headers, CTAs primarios, brand mark, botones. |
| **terra-500** | Naranja vivo FitoLink oficial del ecosistema (`#d45220`). El mismo del botón "Empieza gratis →" de la LandingPage. Sustituye al efímero `agrom-terra #E07A3C`. Usado para CTAs warm, acentos de marca, highlights. |
| **earth-50 / earth-100 / earth-300** | Neutros cálidos tierra del ecosistema. `earth-50 #fdf8f0` = paper (fondo cálido). `earth-100 #f5e6cc` = parch (bloque diferenciado). `earth-300 #d4a85a` = rule (hairlines). Sustituyen a `agrom-paper/parch/rule`. |
| **Tipografía oficial ecosistema** | Display: **Instrument Serif** (Google Fonts, ital@0;1) — títulos H1/H2 + display protagonistas. Body: **DM Sans** (400-700) — default global. Eyebrows técnicos: **IBM Plex Mono** (400-600) — `§ 01 · ESTADO DE SUS PARCELAS`. Sustituye al stack efímero Fraunces + IBM Plex Sans del Identity Sprint AgroM v0.1. Configurada en `tailwind.config.js` (`font-display`/`font-body`/`font-mono`) y replicada en `emailService.ts` (`FONT_DISPLAY`/`FONT_BODY`/`FONT_MONO`). |
| **Jerarquía marca · AgroM empresa + FitoLink producto** | Decisión JuanCho 14-may-2026 (Opción A). **AgroM** es la empresa, **FitoLink** es uno de sus productos (el SaaS satélite + dron). Regla de oro para el copy: relación humana / sello / certificación / programa / equipo de contacto → siempre AgroM. Panel / módulo / sección del dashboard / tab del producto → FitoLink. Aplicada en navbar landing, footer, badge proveedor "AgroM ✓", "El equipo de AgroM te contactará", "Empresa certificada por AgroM", "Un experto de AgroM te contactará", "programa AgroM para cooperativas". Las pestañas internas del producto ("Red FitoLink", "Servicios FitoLink", "Panel de control · FitoLink AGDP") conservan FitoLink porque son nombres de sección del producto, no acciones de empresa. |
| **fitolink.agrom.es** | Subdominio operativo del SaaS bajo dominio empresa, en línea con la jerarquía AgroM/FitoLink. Estado 18-may-2026: **completamente funcional** — DNS A directo a `187.77.71.102`, cert Let's Encrypt válido (`CN=fitolink.agrom.es`, issuer R13), nginx server block configurado en el VPS. Es la URL canónica del producto desde 18-may. Historial: 14-may bloqueado por `Hostinger ghost record` (record A fantasma coexistiendo con CNAME nuevo). Resuelto en algún momento entre 14 y 18 may (probablemente ticket Hostinger + propagación). Arquitectura DNS final: `agrom.es` landing institucional (V2), `fitolink.agrom.es` SaaS (operativo), `agroops.agrom.es` consultoría (V2). |
| **Hostinger ghost record** | Bug del panel DNS de Hostinger detectado 14-may-2026. Al editar/borrar un A record desde el panel, el cambio se refleja en la UI pero **no** llega al zone file publicado por los nameservers autoritativos (`apollo.dns-parking.com`, `athena.dns-parking.com`). El record fantasma se publica junto al nuevo, generando configuración DNS inválida. Workaround intentado: delete + recreate, también afectado. Solución única: ticket soporte Hostinger para limpiar zone file manual. Lecciones: (1) verificar SIEMPRE con `dig @nameserver_autoritativo dominio ANY` antes de asumir cambio aplicado; (2) cuidado al editar DNS en cuenta Hostinger con múltiples dominios — el selector de dominio en la parte superior es fácil de confundir, llevó al borrado accidental del A record `fitolink.systemrapid.io` durante el trabajo en `agrom.es`. |
| **AgroM ✓ badge** | Sello visual en cards de Marketplace que indica proveedor verificado por la empresa AgroM. Antes "FitoLink ✓" (incorrecto, asignaba la certificación al producto). El sello es de empresa, no de producto. Cambiado 14-may en `MarketplacePage.tsx`. |
| **fitolink.systemrapid.io · retirado 18-may-2026** | Antigua URL del SaaS (dominio del brazo técnico SystemRapid). Decisión JuanCho 18-may al regresar del viaje: *"fitolink.systemrapid.io no, para nada, eso muere"*. No se mantiene DNS, no se pone redirect 301. El dominio simplemente deja de existir. Razón: la jerarquía marca (AgroM empresa + FitoLink producto) hace que el SaaS deba vivir bajo `fitolink.agrom.es`, sin doble identidad confusa. Sweep aplicado en `STAGING_BASE` (emailService.ts), TermsPage, build scripts comerciales (`build_informe_pistachar.py`, `build_onepager.py`), README, OVERWATCH, CLAUDE.md. Email digest y alertas ahora linkan a `fitolink.agrom.es`. El nginx del VPS conserva el server block para `systemrapid.io` (no estorba), pero al no haber DNS apuntando, no se sirve a nadie. |
| **plantingYear · calibratingUntil · ESTABLISHMENT_YEARS** | Sprint Calibración del Cultivo 18-may. Cuando el agricultor sube una parcela nueva, opcionalmente informa `plantingYear` (selector año). Si lo informa, el sistema deduce automáticamente `establishmentPhase` usando la tabla `ESTABLISHMENT_YEARS` (pistacho 7, olivo 5, almendro/cítrico/frutal 4, viñedo 3) y `calibratingUntil=null` (sistema calibrado al instante). Si NO lo informa, `calibratingUntil = hoy + 60 días` (modo Calibración pasiva: el pipeline/UI/digest se suavizan mientras el sistema acumula lecturas Sentinel-2 y aprende el patrón). Helpers `inferEstablishmentFromAge`, `isCalibrating`, `calibrationDaysLeft` en `packages/shared/src/cropContext.ts`. Aplicado en `parcelService.createParcel` + `createParcelsBulk` vía `applyCalibrationOnCreate`. |
| **Aula Jaén Jesús · cuenta demo pedagógica** | Cuenta sintética del profesor Jesús Vivar (Agricultura Precisión, Jaén) creada 18-may-2026 para uso en sus clases universitarias. `googleId: jesus-vivar-edu` · `jesusvivar22@gmail.com`. Botón demo "Aula Jaén Jesús" en LoginPage. 6 parcelas hand-crafted ilustrando escenarios canónicos: (1) olivar tradicional sano Sierra Mágina, (2) olivar de seto intensivo Úbeda, (3) pistachar joven CALIBRANDO sin plantingYear Sierra Sur, (4) almendro establecimiento INFORMADO plantingYear=2024 Cazorla, (5) olivar con estrés hídrico real La Loma (alerta crítica activa + Operation completada showing trazabilidad), (6) olivar heterogéneo Andújar (media OK, intra-parcela revela zona crítica). Seed: `apps/api/src/seed/seedProfessorDemo.ts`. Compartida — múltiples estudiantes pueden usar el mismo botón. Comercialmente: cada curso del profesor expone FitoLink a ~30 alumnos que mañana serán agrónomos/asesores con poder de recomendación. |
| **Parcel.isSyntheticDemo** | Flag boolean opcional en el schema Parcel. `true` marca parcelas pedagógicas/demo con datos hand-crafted (caso Aula Jaén Jesús). El pipeline V2 (`workers/geo-pipeline/src/pipeline.py`) y el constructor MPC (`build_mpc_baselines.py`) saltan estas parcelas — no consultan Copernicus, no reescriben `ndviHistory`, no generan alertas automáticas. Protege la coherencia narrativa de los escenarios pedagógicos. Si en el futuro tenemos más cuentas demo (Aula Sevilla, Aula Murcia, etc.) usan este mismo flag. |
| **SoilGrids 250m · ISRIC** | Dataset global de propiedades estáticas del suelo a 250m de resolución, mantenido por ISRIC (International Soil Reference and Information Centre). Capas por profundidad (0-5, 5-15, 15-30, 30-60, 60-100, 100-200 cm): arcilla %, arena %, limo %, carbono orgánico, bulk density, capacidad agua disponible, pH, CIC. Decidido 22-may-2026 como **próxima integración** prioritaria tras Sprint Calibración del Cultivo, antes que SMAP/Sentinel-1 SAR. Razón: añade dimensión NUEVA (textura estructural del suelo) que multiplica el valor interpretativo de los datos recurrentes ya integrados — un NDVI 0.42 sobre arcilla 62% no significa lo mismo que sobre arena 70%. Caso pedagógico oro (Aula Jaén Jesús, parcela #06 olivar heterogéneo). Diferenciador competitivo: Hemav/xFarm/Auravant no exponen SoilGrids en UI. Comercial: factor de riesgo para aseguradora + caracterización cartera para cooperativa. |
| **soilService.ts · Parcel.soil** | Implementación SoilGrids 22-may-2026 (`apps/api/src/services/soilService.ts`). Cliente REST API ISRIC sin auth. `fetchSoilProfileForPoint(lat, lng)` agrega capas 0-5/5-15/15-30 cm con pesos por espesor (5+10+15), convierte unidades crudas (g/kg / dg/kg / cg/cm³ / 0.1 cm³/cm³ → unidades agronómicas), renormaliza texturas a sumar 100. `dominantTextureUSDA()` simplifica 12 clases USDA en 7 familias agronómicas castellano (arcilla / arcilla arenosa-limosa / franco arcilloso variantes / franco arenoso-limoso / franco / arenoso / limoso). Endpoint POST `/parcels/:id/soil/refresh` manual. Auto-fetch fire-and-forget en `createParcel` + `createParcelsBulk` vía `maybePopulateSoil()`. A diferencia de enrichments MPC (temporales), suelo es PERMANENTE — fetch una vez, queda en BD. |
| **SoilProfileCard · § SUELO · SOILGRIDS 250M** | Card UI en ParcelDetailPage que renderiza el `Parcel.soil`. Header `bg-brand-600` + eyebrow mono. Banner principal con textura dominante (Franco / Arcilla / etc.) + 3 barras horizontales arcilla/arena/limo con porcentajes tabular-nums. 3 métricas agronómicas en grilla: cap. agua disponible volumétrica, carbono orgánico g/kg, densidad aparente g/cm³. Footer técnico citando ISRIC + coords centroide + botón "Refrescar" manual. Empty state cuando `soil === undefined` con CTA "Calcular perfil de suelo" → POST refresh endpoint. Si la API ISRIC falla en auto-fetch (al crear parcela), el card muestra empty state y el usuario puede dispararlo manualmente. |
| **fire_proximity · NASA FIRMS · fireService** | Sprint FIRMS 04-jun-2026. Nuevo ALERT_TYPE `fire_proximity` en shared constants. `apps/api/src/services/fireService.ts` cliente NASA FIRMS API combinando VIIRS Suomi-NPP + VIIRS NOAA-20 (375m resolución, ~3h latencia, cuasi-tiempo-real). `env.FIRMS_MAP_KEY` requerido (registro gratis NASA EARTHDATA, sin uso comercial restringido). `fetchActiveFiresNearGeometry(geometry, radiusKm, days)` filtra por distancia haversine desde centroide del polígono. Default filtro contra confidence='l' (reflexos industriales). `Alert` model extendido con `fireProximityKm` + `fireSource`. ndviValue/ndviDelta ahora con default 0 (no aplican a fire alerts). Severity inferida: critical si <2km, high si 2-5km. Crítico en verano mediterráneo (Andalucía Plan INFOCA, julio-septiembre temporada incendios). |
| **FireProximityCard · § FOCOS TÉRMICOS · NASA FIRMS VIIRS** | Card UI en ParcelDetailPage que consulta FIRMS y muestra focos cerca de la parcela. Banner ROJO destacado si hay foco <5km. Lista ordenada por distancia con km, fecha relativa (hoy/ayer/hace N días), hora UTC, satélite (Suomi-NPP / NOAA-20), brillo K, FRP MW, confianza, día/noche, lat/lng. Empty state suave "Sin focos activos en su zona". Recomienda verificar con 112 / Plan INFOCA si proximidad crítica. React Query staleTime 1h (NASA actualiza ~3h). Integrada en digest matutino: si fires<5km, sección "§ 00 · FOCOS TÉRMICOS CERCANOS" al inicio del email + prefijo ⚠️ en subject. |
| **SigpacViewerPage · /sigpac** | Visor SIGPAC público sin login en `fitolink.agrom.es/sigpac` (Sprint 04-jun-2026). Alternativa al horrible visor oficial MAPA (UI 2010, lento, no compartible, no responsive). Form 7 campos catastrales SIGPAC. Mapa Leaflet 460px con tiles Esri World Imagery (mucho mejor que el oficial). Polígono catastral en **terra-500 naranja brand** (visible sobre satellite vs verde brand que se camuflaba). Card "DATOS DEL RECINTO" con superficie/uso traducido/referencia + botón sólido "Buscar otra parcela" arriba derecha. Sección "§ TIEMPO EN SU PARCELA · HOY" con Open-Meteo (lead magnet anónimo · gratis HOY, banner candado para 7 días detrás del registro). URL parametrizada `/sigpac/:prov/:muni/:agre/:zona/:pol/:par/:rec` para enlaces compartibles SEO-friendly (cada parcela genera URL única indexable). Link "Visor SIGPAC" en navbar + footer LandingPage. Endpoint `/api/v1/sigpac/lookup` ya era público (sin auth). Patrón inspirado DroneHub ("Zonas de Vuelo" en navbar). Diferenciador SEO + lead viral via WhatsApp share. |
| **Aula Jaén v2 · SIGPAC catastral real** | Cierra el sprint Aula Jaén iniciado 18-may tras 4 iteraciones v1.x con polígonos sintéticos. v1.x (cuadrados → rectángulos rotados → octógonos perturbados perpendicularmente) tenía siempre el mismo problema raíz: coordenadas inventadas pinchaban cortijos en mitad de fincas. v2 (22-may, commit f4cc352) reemplaza el sistema `realisticPolygon` por descarga directa de geometrías catastrales oficiales desde SIGPAC vía `fetchByReference()` del sigpacService ya existente. 6 referencias catastrales reales Jaén verificadas y sondeadas con API antes de meter al seed: Albanchez Sierra Mágina (23/1/0/0/10/15/1 OV 1.92ha) · Sabiote La Loma (23/74/0/0/3/1/1 OV 5.71ha) · Frailes pistachar (23/31/0/0/3/5/1 FY 2.47ha) · Quesada almendro (23/70/0/0/3/5/1 FY 0.38ha) · Mancha Real olivar estrés (23/61/0/0/3/5/1 OV 2.43ha) · Andújar Sierra Morena (23/5/0/0/15/5/1 OV 1.29ha). Cualquier alumno de Jesús Vivar puede pegar la ref en el visor SIGPAC oficial y ver EXACTAMENTE la misma geometría. Cero invento. Auditable. Patrón AgroM: ciencia auditable, no marketing. Eliminados helpers obsoletos `mulberry32` y `realisticPolygon`. |
| **ADV · Agrupación de Defensa Vegetal** | Asociación de agricultores del mismo cultivo + comarca que comparten 1-2 técnicos agronómicos asalariados para vigilancia de plagas y asesoría fitosanitaria. Reguladas por RD 1075/2014 + normativa autonómica. Estructura típica: 30-200 socios cubriendo 500-5000 ha. Reportan a RAIF (Andalucía) o equivalentes regionales. Identificado 04-jun-2026 como buyer institucional ALTAMENTE PRIORITARIO para campaña 2026 — match perfecto con stack FitoLink sin código nuevo casi: PestAdvisories + match comarca + alertas tempranas NDVI + digest matutino + multi-parcela (vista cooperative). Esfuerzo técnico mínimo: añadir rol `adv` + UI labels (~½ jornada). Modelo de datos cabe en `cooperative` ya existente (1 técnico admin + N socios farmer linked vía cooperativeId). En España hay 600+ ADVs activas, alta densidad en olivar Andalucía. Decisor compra: el técnico (sufre el dolor) + junta directiva (firma). |
| **Comunidad de Regantes** | Asociación gestora de riego colectivo y reparto de agua entre socios agrícolas. ~700 comunidades en España, gestionan riego de millones de hectáreas. Identificada 04-jun-2026 como buyer institucional ⭐⭐⭐ prioritario campaña 2026: la sequía 2024-2026 + **RD 950/2024** (reducción obligatoria consumo agua agraria) + reparto eficiente entre socios genera dolor regulatorio agudo. Stack FitoLink alineado con su decisión clave (cuándo regar a quién): NDVI + thermal LST + ERA5 drought signal + Open-Meteo. Pendiente: dashboard "Estado hídrico cartera comunidad" + opcional Sentinel-1 SAR (humedad bajo nubes). Foco geográfico campaña 2026: Andalucía + Levante. |
| **Decisión PM · Campaña 2026 · Aseguradoras aparcadas** | Decisión 04-jun-2026 sobre segmentación buyer campaña 2026. **Aseguradoras APARCADAS hasta 2027+**: ciclo comercial 5+ años (peritaje tradicional, regulación Agroseguro, juntas conservadoras), ROI no retorna en 12 meses. Foco campaña 2026 en 4 buyers institucionales con dolor AHORA y ciclo decisión corto: **(1) Agricultor Tipo A/B**, **(2) Cooperativa olivar mediana**, **(3) Comunidad de Regantes** (sequía + RD 950/2024), **(4) ADV**. Regla operativa nueva: **"el launch va con lo nuestro"** — no bloquear MVP por dependencias externas no validadas (caso Elevenais blockchain eIDAS aparcado). APIs externas SÍ aceptables sin riesgo: organismos científicos/gubernamentales públicos + gratuitos (Copernicus, MPC, NASA FIRMS, ISRIC, SIGPAC, Open-Meteo). NO partners comerciales privados con contrato bilateral en path crítico. |
| **Visor SIGPAC · § COMPARTIR Y EXPORTAR** | Card nueva en `SigpacViewerPage.tsx` (05-jun-2026, commit 71ec2ee) con 4 botones de acción que multiplican el valor del lead magnet para tres perfiles distintos: (1) **WhatsApp** botón verde #25D366 con `wa.me/?text=...` y texto preformateado (📍 SIGPAC ref · uso · Ha · URL del visor + firma "vía AgroM · FitoLink") para viralidad pasiva en grupos cooperativa; (2) **Google Maps** botón gris oscuro abriendo el centroide del polígono en `maps.google.com/?q=lat,lng` para piloto-dron/agricultor navegando turn-by-turn a la finca; (3) **Descargar KML** botón brand-600 generando KML 2.2 OGC con style terra-500 (compatible DJI GS Pro/Litchi/DJI Pilot/Google Earth/DroneDeploy/Pix4D/QGIS); (4) **Descargar GeoJSON** botón earth-200 generando Feature con properties (sigpacRef, cropUseLabel, areaHa, source) para QGIS/asesor agrónomo. Helpers JS internos: `buildKmlFromGeometry(geometry, ref, cropUseLabel, areaHa)` genera KML válido OGC con coordenadas `lng,lat,0` separadas por espacio + style AABBGGRR (KML usa orden inverso bytes vs HTML); `escapeXml()` sanitiza nombres con caracteres especiales; `downloadBlob(content, filename, mime)` helper compartido descarga blob vía Blob + createObjectURL + click sintético, con cleanup retardado para Safari. Sin cambios backend. Microcopy footer explica para qué sirve cada acción ("KML para DJI GS Pro, Litchi, Google Earth · GeoJSON para QGIS, ArcGIS, asesor agrónomo · Google Maps abre el centroide para navegar a la finca"). |
| **AdvDashboardHome · rol `adv` · /dashboard** | Dashboard inicial del rol ADV (Agrupación de Defensa Vegetal) en `apps/web/src/features/adv/AdvDashboardHome.tsx` (Sprint Rol ADV · 05-jun-2026). Componente con KPI strip de 4 métricas (Socios vigilados / Hectáreas vigiladas / Avisos activos en comarca / NDVI medio comarca) + mapa agregado con parcelas de todos los socios coloreadas por NDVI + lista de socios con NDVI individual y conteo de avisos. Reusa endpoint `/cooperative/overview` (mismo shape estructural: entidad agregadora con N farmers vinculados via `cooperativeId`). Header editorial con prefijo "§ VIGILANCIA COMARCAL · ADV" y banner crítico animado cuando hay avisos críticos. Empty state específico ADV con onboarding 3 pasos editoriales (1. Comparta enlace alta con socios · 2. Nuestro equipo los vincula en 24h · 3. Reciba digest comarcal a las 7h). Cuando hay >0 socios, copy del header habla del rol institucional ("vigilancia colectiva") en vez de "mis parcelas". V2 (post-piloto ADV): generador automático RAIF cuatrimestral PDF + gestor de socios self-service + calendario visitas campo + mapa formal alertas FitoLink ↔ tabla plagas RAIF. |
| **Rol `adv` · USER_ROLES** | Nuevo rol añadido a `packages/shared/src/constants.ts` (05-jun-2026): `USER_ROLES = [farmer, pilot, agronomist, insurer, admin, cooperative, adv]`. Estructuralmente parecido a `cooperative` (agrega farmers vía `cooperativeId` link en User), semánticamente distinto: cooperative = comercialización (venta cosecha), adv = sanidad vegetal (vigilancia plagas + reporte RAIF + cuota anual a justificar). NO auto-verified (igual que cooperative/pilot/insurer) — `authService.isAutoVerified` es `false`, requiere verificación manual del equipo SR (24-48h) antes de operar. SÍ recibe digest matutino (`digestService` líneas 212 + 386 extienden elegibilidad a `farmer ∨ cooperative ∨ adv`) — vigilancia comarcal diaria es justamente el use case del rol. RegisterPage con opción ADV (icon `/provider-cooperative.svg` provisional, V2 traerá icon set propio escudo+planta). AdminUsersPage con badge `bg-terra-100 text-terra-700` (naranja brand AgroM) para diferenciar visualmente de cooperative (amber) y farmer (green). NAV_ITEMS.adv mínimo: Inicio + Proveedores (V2: Socios, RAIF, Visitas campo). |
| **CooperativeMembersPage · /dashboard/cooperative/socios** | Segunda pestaña del rol `cooperative` (Sprint Onboarding Cooperativa básico · 05-jun-2026). Antes el nav cooperative solo tenía "Inicio" + "Proveedores" — daba sensación de "demasiado simple" para un prospect. Ahora "Socios" es segunda entrada editorial con detalle por agricultor de la cartera. Reusa endpoint `/cooperative/overview` (mismo `members[]` que ya consumía CooperativeDashboardHome). Filtros (Todos / Con avisos / Críticos) + sort configurable (Urgencia / Nombre A-Z / Ha mayor primero / NDVI peor primero). Card de socio con: rank index, avatar iniciales, badge alerta count si >0, parcelas + Ha, NDVI tabular con color umbral (rojo<0.30 / terra<0.45 / amarillo<0.55 / verde≥0.55) + label castellano (crítico / bajo / medio / sano), CTA "Ver →" (V2 navegará a vista parcelas-del-socio). Banner editorial "§ PRÓXIMAS MEJORAS · CARTERA" sugiriendo V2 honestamente: alta/baja socios, export PDF/CSV cartera, contacto WhatsApp/email un-click, vista detalle socio. |
| **Refactor `seedCooperativeSocios.ts` con upsert de cooperativa** | Sprint Onboarding Cooperativa básico · 05-jun-2026. Antes el script fallaba con `process.exit(1)` si el usuario `demo-cooperative-001` no existía previamente — y ese usuario nunca era creado automáticamente porque (a) seed.ts principal lo omitía, (b) en producción seed.ts está skip-eado por guard de "real pipeline data detected" cuando hay snapshots>3. Resultado: la cooperativa demo nunca se sembraba en prod, el botón "Cooperativa" del LoginPage daba 401, el rol cooperative era invendible. Refactor: el script ahora hace UPSERT del usuario cooperativa al inicio (email `cooperativa@dcoop-demo.coop`, name "Cooperativa Demo · Olivar DOP Estepa", company "DCOOP Demo (sintética)", isVerified, avatar `/provider-cooperative.svg`, role 'cooperative'). Luego procede con los 5 socios + 10 parcelas como antes. Idempotente. Ejecutar con `docker compose exec -T api node apps/api/dist/seed/seedCooperativeSocios.js`. |
| **AdvDashboardHome · bug fix campos endpoint** | 05-jun-2026 · descubierto al construir CooperativeMembersPage. AdvDashboardHome.tsx (commit e67768d) asumía que el endpoint `/cooperative/overview` devolvía `kpis.totalHa` + `kpis.avgNdvi` + `members[].totalHa` + `members[].avgNdvi`. El endpoint real devuelve `areaHa` + `ndviAvg` en ambos niveles. Los KPIs del dashboard ADV en producción renderizaban "—" silenciosamente porque los campos no coincidían y TypeScript permitía el acceso con `any` casts. Fix: alinear interfaces y todos los accesos a los nombres reales (`areaHa`/`ndviAvg`). Test post-deploy: login como usuario ADV vinculado a cooperative debería mostrar KPI strip con valores reales en vez de "—". Aprendizaje: usar el tipo exportado del servicio (`CooperativeOverview` desde `cooperativeService.ts`) en frontend para no duplicar shapes que pueden divergir. |
| **Rol `regantes` · USER_ROLES** | Sprint Comunidad de Regantes · 05-jun-2026. Añadido a `packages/shared/src/constants.ts`: `USER_ROLES = [farmer, pilot, agronomist, insurer, admin, cooperative, adv, regantes]`. Buyer institucional ⭐⭐⭐ campaña 2026 por sequía + RD 950/2024 (reducción obligatoria consumo agua agraria). Estructuralmente reusa `cooperativeId` (un usuario `regantes` agrega farmers vía ese campo, igual que cooperative/adv). Diferenciación semántica: cooperative = comercialización cosecha, adv = sanidad vegetal, regantes = gestión hídrica colectiva. Backend `cooperativeService.getOverview` acepta los 3 roles agregadores. Route `/v1/cooperative/overview` autoriza `cooperative ∨ adv ∨ regantes ∨ admin`. `digestService` extiende elegibilidad a los 4 roles principales (`farmer ∨ cooperative ∨ adv ∨ regantes`) — gerente regante recibe digest matutino con prioridad de riego. AdminUsersPage badge `bg-sky-100 text-sky-700` (azul agua, coherente con dominio hídrico). NO auto-verified (requiere validación 24-48h). Foco geográfico campaña 2026: Andalucía + Levante. |
| **RegantesDashboardHome · /dashboard (rol regantes)** | Componente nuevo `apps/web/src/features/regantes/RegantesDashboardHome.tsx` (Sprint Comunidad de Regantes · 05-jun-2026). Reusa endpoint `/cooperative/overview` (mismo shape). Copy editorial específico riego: header "§ ESTADO HÍDRICO · CARTERA REGABLE". KPI strip de 4 métricas distintas a Coop/ADV: **Hectáreas regables** (cartera total), **% parcelas en estrés** (NDVI<0.40, umbral conservador 0.40 vs el 0.30 crítico — el gerente regante quiere visibilidad temprana, no solo lo que ya está perdido), **Críticas (riego urgente)** (NDVI<0.30, reusa `kpis.parcelsBelowCritical`), **NDVI medio cartera**. Header con banner animated terra si %estrés>30%. Mapa agregado parcelas socios. Lista lateral "§ PRIORIDAD DE RIEGO" ordenada por urgencia hídrica (alertas + NDVI bajo) en lugar de NDVI absoluto. Top 3 destacados con fondo terra-500/5. Empty state editorial 3 pasos. V2: reparto sugerido m³/ha por socio + cuaderno digital riegos + match con tabla aforos comunidad. |
| **seedRegantesSocios.ts · zona regable Levante** | Seed dedicado (`apps/api/src/seed/seedRegantesSocios.ts`) · 05-jun-2026 · idempotente. UPSERT de la Comunidad de Regantes demo (`demo-regantes-001`, role='regantes', name "Comunidad de Regantes Demo · Vega Baja", company "Comunidad de Regantes Demo (sintética)"). Luego 4 socios farmer + 8 parcelas (2 por socio) con `cooperativeId` apuntando a la comunidad. Zona geográfica: **Levante regable** (Vega Baja del Segura: Alicante + Murcia + Ribera del Júcar Valencia) — coherente con el buyer real (la pelea 2026 es la sequía + RD 950/2024 en zona regable, no en olivar Estepa donde está la coop). Cultivos: cítricos + frutal de hueso + hortícolas — los más dependientes de riego. v3 (commit 691b864): SIGPAC catastral real (8 refs verificadas Vega Baja del Segura) sustituye los polígonos sintéticos cuadrados de v1. Ejecutar con `docker compose exec -T api node apps/api/dist/seed/seedRegantesSocios.js`. |
| **irrigationDecisionService · /parcels/:id/insights/irrigation-decision** | Servicio backend (`apps/api/src/services/irrigationDecisionService.ts`) · Sprint BUMM Regantes · 05-jun-2026 · commit ec53d21. Convierte 7 cards técnicas (NDVI + textura suelo + clima + cultivo) en UNA decisión de riego clara para gerente regantes/coop/adv. Basado en FAO-56 simplificado: ET cultivo = ET0 × Kc (Kc por crop type, FAO-56 mid-season; ET0 desde `recentClimate.et0TotalMm` ERA5 si existe, sino Hargreaves por temperatura). Demanda total = ET × 14d. Oferta = lluvia×0.8 + reserva_suelo×0.5 (50% AWC utilizable, AWC desde SoilGrids fieldCapacityVol × 30cm). Déficit = max(0, demanda − oferta). Cupo m³ = déficit_mm × areaHa × 10. **Cupo RD 950/2024 = cupo × 0.80** (reducción 20% norma agraria, dato que vende al gerente). Urgency: urgent (NDVI<0.30), soon (NDVI<0.45 + descendente), monitor (déficit>40mm), sufficient. Devuelve además `analysisNarrative` (ver entry siguiente). |
| **composeNarrativeInsight · plantillas inteligentes server-side · NO LLM** | Función en `irrigationDecisionService.ts` (commit 0deef97) que genera narrativa editorial de 3-5 frases cruzando 6-8 variables. **NO usa LLM**: plantillas determinísticas alimentadas con datos reales del backend. Cero alucinación. Cero coste por request. Coherente con regla CRITICAL_no_inventar. Variables cruzadas: (1) estado cultivo según rango NDVI, (2) tendencia solo si notable, (3) suelo + textura + días retención (helper `retentionDaysByTexture`: arcilla=7d, franco=4d, arenoso=2d), (4) lluvia reciente acumulada 14d, (5) fenología por cultivo+mes (helper `phenologyNoteFor` cubre cítrico/frutal/olivo/pistacho/almendro/viñedo en sus fases críticas), (6) establecimiento si plantingYear reciente, (7) cierre operativo según urgency. Ejemplo: "El frutal está en estrés agudo (NDVI 0.27, zona crítica). Caída notable respecto a la lectura anterior (-0.03). Apenas ha llovido en las últimas 2 semanas (0 mm). Fase de cuajado y crecimiento del fruto, alta sensibilidad al estrés hídrico. Acción inmediata recomendada para evitar pérdida de cosecha." Se muestra en sección "§ ANÁLISIS INTEGRADO" del banner DECISIÓN DE RIEGO entre justificación y grid. Decisión PM: empezar con plantillas (sin LLM) por cero riesgo; si en 3-6 meses con >10 clientes vale la pena diferenciarse migrar a LLM con grounding estricto + disclaimer. |
| **IrrigationDecisionBanner · § DECISIÓN DE RIEGO** | Componente UI (`apps/web/src/features/parcels/IrrigationDecisionBanner.tsx`) · commit ec53d21 + 0deef97. Banner editorial AgroM montado en ParcelDetailPage condicional al rol (`IrrigationDecisionForRole` wrapper · solo para regantes/cooperative/adv · farmer NO lo ve). Estructura: header con eyebrow "§ DECISIÓN DE RIEGO" + emoji urgency + titular `recommendedAction` + justificación corta + **sección "§ ANÁLISIS INTEGRADO"** con `analysisNarrative` (border-l brand-600) + grid 4 variables (NDVI actual + tendencia, déficit mm, ET cultivo mm/d, reserva suelo mm) + **bloque cupo m³** (bg-earth-100 + border brand-600 cuando cupo>0; mensaje corto "Sin déficit hídrico significativo" cuando cupo=0) con cupo libre y cupo RD 950/2024 −20% destacado terra-500 + alternativa si aplica + footer trazabilidad metodología FAO-56. Color scheme por urgency: urgent=rojo, soon=terra, monitor=amarillo, sufficient=verde. |
| **reportService · pdfkit nativo · cartera PDF/CSV** | Servicio backend (`apps/api/src/services/reportService.ts`) · Bloque B · 05-jun-2026 · commit 5fd3597. Genera reportes editoriales AgroM para descarga desde dashboards agregados (coop/adv/regantes). Stack pdfkit (~70KB nativo Node) descartando Puppeteer (+300MB Chromium en Docker build). `generateCarteraPdf(input)` devuelve Readable stream que el controller hace pipe al response. Estructura PDF: header eyebrow + hairline terra + titular entidad + fecha generación en castellano + sección "§ INDICADORES CLAVE" en grid 2×3 + sección "§ DETALLE POR SOCIO" tabla ordenada por urgencia (alertCount desc + ndviAvg asc) + footer con trazabilidad fuentes (Sentinel-2 + ISRIC + ERA5 + SIGPAC) + cita AgroM/FitoLink. Salto página automático para listas largas. Paleta AgroM replicada en hex (pdfkit no entiende Tailwind). `generateCarteraCsv(input)` devuelve string con BOM UTF-8 + meta-cabecera #-prefixed (Excel "Texto a columnas" la ignora) + header bilingüe 12 columnas + escape RFC 4180. Endpoint `/cooperative/report?format=pdf\|csv` reusa el shape del overview, autoriza con mismo aggregator-rule. Filename normalizado por entityName + date. Componente compartido `DownloadReportButtons.tsx` (axios responseType:blob + createObjectURL + click sintético) montado en 3 dashboards. NOT INCLUIDO en este sprint: reporte RAIF cuatrimestral específico ADV + reporte reparto hídrico regantes (V2). |
| **getParcelById · aggregator-rule auth extendida** | Fix backend `parcelService.getParcelById` · commit ec53d21. Antes solo autorizaba `owner === userId` o `admin` o `insurer-cartera`. Si un usuario rol regantes/cooperative/adv intentaba abrir `/parcels/:id` de una parcela de uno de sus socios → forbidden → frontend mostraba "Parcela no encontrada". Bug latente desde Sprint Regantes. Nueva regla `isAggregatorOfOwner`: si `userRole ∈ [cooperative, adv, regantes]` Y el owner de la parcela tiene `cooperativeId === userId` (es socio del agregador), autorizado. Mismo principio que `isInsurerCartera`. Aplicado en todos los endpoints insight que llaman getParcelById (`/insights/ndvi-forecast`, `/insights/weather-events`, `/insights/irrigation-decision`, `/fires`, `/soil/refresh`). |
| **HITO 05-jun-2026 · Cero invento · 26 parcelas demo SIGPAC catastral real** | Tras commit 4ecbffb (Coop v2 SIGPAC), **TODAS las parcelas demo del producto FitoLink son catastrales reales auditables**. Resumen: Aula Jaén Jesús (6 refs prov 23 Jaén · commit f4cc352) + Cooperativa Estepa (10 refs prov 41 Sevilla DOP Estepa · commit 4ecbffb) + Regantes Vega Baja (8 refs prov 03 Alicante · commit 691b864) + Antonio agricultor (2 refs prov 41 Sevilla · seed.ts original) = **26 parcelas, 3 provincias, 100% verificables en el visor SIGPAC oficial MAPA**. Cero polígonos sintéticos `ringFromCentroid` (helper eliminado del flujo). Patrón consistente: `fetchByReference(prov, muni, agre, zona, poligono, parcela, recinto)` del sigpacService. Cualquier prospect/cliente puede pegar cualquier ref en https://sigpac.mapa.gob.es/fega/visor/ y ver EXACTAMENTE la misma geometría. Cumple regla CRITICAL_no_inventar al 100%. Diferenciador comercial fuerte: "ningún competidor enseña polígonos catastrales reales en demo, todos usan cuadrados sintéticos". |
| **Marco normativo dron · 3 BLOQUES acumulativos · scope AgroOps (NO FitoLink)** | **DECISIÓN PM JuanCho 05-jun-2026**: el cumplimiento normativo operativo de tratamientos con dron es competencia del producto **AgroOps** (aparcado mientras sacamos FitoLink), NO de FitoLink (SaaS satélite + IA + alertas). FitoLink informa el QUÉ (estado cultivo, decisión riego, focos térmicos); AgroOps cubrirá el CÓMO operativo + cumplimiento legal cuando se reanude. Aquí queda capturado como ground truth interno para cuando AgroOps se desaparque o cuando AgroM como empresa necesite verificar requisitos para una operación comercial. Captura del contexto recibido del chat WhatsApp técnico (inspectores ITEAF en activo). NO copy comercial. **Bloque 1 · Seguridad aérea AESA/EASA** · Reglamento (UE) 2019/947 + RD 517/2024 · operador UAS registrado + categoría ESPECÍFICA + PDRA-S01[F] (desde mayo 2024 para drones hasta 3m, simplifica vs SORA completo) + DRI activo (obligatorio desde 1/1/2024) + manual operaciones + seguro RC aeronáutico + piloto competencia A2 o equivalente SORA. **Bloque 2 · ITEAF inspección equipos** · RD 1702/2011 · el dron es equipo de aplicación de fitosanitarios y debe censarse en MAPA (ROMA en mayoría de CCAA, REGANIP en Extremadura) · primera inspección a los 5 años desde fecha factura compra, luego cada 3 años · ISO 16122-5 aún en desarrollo (laguna técnica reconocida) · NO aplica si solo se aplica abono o OCB · seguro RC fitosanitario adicional al aeronáutico. **Bloque 3 · Uso fitosanitarios** · Directiva 2009/128/CE + RD 1311/2012 · art. 27 PROHÍBE aplicación aérea con carácter general · requiere autorización EXCEPCIONAL del órgano competente CCAA para CADA tratamiento (no genérica, no anual) + plan aplicación Anexo VI + producto autorizado para aplicación aérea según Reglamento (CE) 1107/2009 (etiqueta + Registro Productos Fitosanitarios MAPA) + piloto con carné aplicador inscrito ROPO sección tratamientos fitosanitarios + empresa inscrita ROPO + consulta ENAIRE (drones.enaire.es) espacio aéreo antes de cada vuelo. **Excepción Bloque 3**: OCB (Organismos Control Biológico, RD 951/2014) y bioestimulantes NO sujetos. **Si falla cualquiera de los 3, operación ILEGAL aunque los otros 2 estén OK**. Personas valiosas en el chat: Tomás (inspector ITEAF activo · ofrece ayuda) + Alejandra Sandoval (UCLM/Agróptimum pistachero · autora curso ITEAF drones febrero 2025). Las 2 acciones tuyas (audit Jonh + contactar Tomás/Alejandra) siguen vigentes porque AgroM como empresa SÍ las necesita ya. |
| **Regla operativa · logs con body del error externo · 3 servicios pendientes aplicada** | Cierre task #32 · 05-jun-2026 · commit d65bb0b. Aplicado el patrón canónico (capturar `body.slice(0, 200)` antes del throw cuando `!res.ok`) a los 3 servicios externos backend TypeScript pendientes: **soilService** (ISRIC SoilGrids), **weatherEventsService** (Open-Meteo), **sigpacService** (SIGPAC Hubcloud · excepto 404 que es esperado). Añadido import logger en sigpacService que faltaba. Patrón consistente: `if (!res.ok) { const body = await res.text().catch(() => ''); logger.warn({source, status, body: body.slice(0,200)}, '<service>_fetch_failed'); throw ... }`. Pendiente para otra sesión: workers/geo-pipeline/src/* Python (MPC + Copernicus openEO) con patrón equivalente con loguru. |
| **AgroM bajo paraguas Drovinci · modelo operativo dron** | Acuerdo activo capturado 05-jun-2026: hasta nueva orden, AgroM NO es operador legal independiente de tratamientos con dron. Opera comercialmente bajo el paraguas de **Drovinci** (empresa real con certificaciones AESA + ITEAF + ROPO + seguros propios). Drovinci aparece en BD demo como `demo-pilot-002` (Ana Gomez Ferrer · DJI Agras T10 + Phantom 4 Multispectral · AESA A1/A3 + A2 + ROPO Aplicador). División de responsabilidades: Drovinci aporta cumplimiento legal completo de los 3 bloques (PDRA-S01[F] + ITEAF censo + autorización CCAA por tratamiento + ROPO + seguros). AgroM/FitoLink aporta inteligencia satelital + IA + dashboard + marca comercial + relación cliente + brokerage cliente↔operador + coordinación operativa (vía Jonh). Camino a independencia (no prioridad campaña 2026): AgroM tendría que registrarse como operador UAS propio + censar drones propios MAPA + ROPO empresa + seguros propios · entonces salir del paraguas Drovinci. **Implicación crítica copy comercial**: NUNCA afirmar "AgroM aplica con dron" como operador propio · sí "AgroM coordina tratamientos con drones operados por la red Drovinci" o wording equivalente honesto. Refuerza regla CRITICAL_no_inventar. **Implicación audit legal**: el audit de 3 bloques debe ser con **Ana Gomez Ferrer / Drovinci**, NO con Jonh aislado. Jonh es coordinación operativa de AgroM trabajando con/vía Drovinci. |
| **Sprint C REORIENTADO · Ingesta boletines fitosanitarios oficiales (NO diagnóstico propio)** | Decisión PM JuanCho 06-jun-2026 mañana antes del viaje: el "detector de enfermedades específicas" del backlog (task #31) NO es construir nuestro propio diagnóstico rule-based — sería **inventar** y romperíamos CRITICAL_no_inventar. La aproximación correcta es **replicar información oficial** de los servicios fitosanitarios autonómicos. Cero responsabilidad legal nuestra (la diligencia recae en el servicio oficial), 100% auditable, brutalmente vendible. **Infraestructura YA EXISTE en el repo**: `PestAdvisory` model + `pestAdvisoryService` + `PestAdvisoriesCard.tsx` + endpoint `/parcels/:id/insights/pest-advisories` + integración digest + `seedPestAdvisories.ts` con 2 advisories de muestra RAIF Andalucía (Polilla olivo + Mosca olivo · `source:'RAIF'` + `sourceRef:'Boletín XX/2026'` + `sourceUrl` de la Junta de Andalucía). Lo que FALTA para Sprint C real: (1) **ingesta automatizada RAIF Andalucía** (scraping/RSS del boletín semanal en `juntadeandalucia.es/agriculturaypesca/raif/` · cubre olivar+cítricos+frutal+viñedo Andalucía), (2) **IVIA Comunidad Valenciana** (boletines quincenales · cubre cítricos demo regantes Vega Baja), (3) **GIPA/IRIAF Castilla-La Mancha** (mensual · cubre pistacho Aula Jaén + Jonh), (4) **DARP Cataluña + IMIDA Murcia** cuando lleguen clientes, (5) **tratamiento "estilo FIRMS"** en UI: banner ROJO destacado si advisory crítica activa <radio comarca + cultivo match + sección "§ 00 · AVISO COMARCA" al inicio del digest matutino + prefijo ⚠️ en subject + Alert dedicado con dedupe. Esfuerzo estimado: ~5-6 días con calma a la vuelta del viaje. **NO construir antes del viaje**. Frase clave para copy comercial: "AgroM le avisa cuando el servicio fitosanitario oficial publica una alerta en su comarca". |
| **Regla operativa · logs con body del error externo** | **Establecida 05-jun-2026 tras incidente FIRMS** (Bloque A5). Cuando se llama a una API externa (NASA, Copernicus, ISRIC, MPC, Open-Meteo, SIGPAC...) y el response NO es OK (status≥400), el log de error debe capturar SIEMPRE: (a) status code, (b) source/proveedor, (c) **body del response** (primeros 200 chars). El body es donde los proveedores serios escriben el mensaje real legible ("Invalid MAP_KEY", "Invalid day range. Expects [1..5].", "Rate limit exceeded · retry after 60s", "Bbox out of bounds"). Sin el body, el log es críptico (`status:400` solo) y el debugging tarda horas. Con el body, el diagnóstico es immediato. **Caso real** que justificó la regla: con la KEY de NASA FIRMS actualizada, el sistema seguía sin mostrar focos térmicos. El log antiguo solo decía `status:400` — interpretamos "KEY rota". Tras el fix de log capturando el body, NASA respondió textualmente `"Invalid day range. Expects [1..5]."` y descubrimos que NASA había bajado el max de days de 10 a 5 silenciosamente. Fix en 10 min. Antes del log mejorado, este bug llevaba 24+ horas en prod sin que nadie supiera el motivo. Patrón canónico (TypeScript fetch): `if (!res.ok) { const body = await res.text().catch(() => ''); logger.warn({ source, status: res.status, body: body.slice(0, 200) }, 'external_api_failed'); }`. Aplicar a TODOS los servicios externos: `fireService`, `copernicusClient` (Python), `soilService`, `mpcService`, `openMeteoService`, `sigpacService`. |
| **NASA FIRMS · max day range 5d** | NASA bajó el límite max del parámetro `day_range` del endpoint `/api/area/csv/{KEY}/{SOURCE}/{BBOX}/{DAYS}` de 10 a 5 días entre el Sprint FIRMS original (04-jun-2026) y el día siguiente. Sin aviso público que sepamos. Causaba HTTP 400 con body `"Invalid day range. Expects [1..5]."` cuando pedíamos 7 días (default del digest matutino). Fix 05-jun-2026 (commit 52bada9): clamp `Math.min(5, days)` en `fetchActiveFiresNearGeometry` + defaults a 5d en `fetchActiveFiresNearGeometry` / `fetchActiveFiresNearParcel` / digestService. Ventana 5d es suficiente para detectar focos cuasi-tiempo-real relevantes — un foco térmico de hace >5d ya no es operativo sino histórico. Si NASA cambia el límite otra vez (8d, 3d, lo que sea), el log mejorado (regla "logs con body" arriba) lo detecta inmediatamente. |
